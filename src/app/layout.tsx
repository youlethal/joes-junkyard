import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Joes Junkyard - Professional Business Services',
  description: 'Professional Business services with Modular Design, Custom Branding, Booking System, Gallery, Blog, Reviews, Contact Form. Contact us today for exceptional service.',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}